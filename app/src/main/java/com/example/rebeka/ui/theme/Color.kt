package com.example.rebeka.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Фирменные токены MyGenetics — единственный источник цвета для всего
 * приложения (см. Theme.kt). Совпадают 1:1 с CSS-переменными --mg-* из
 * брендбука (mygenetics-brand-pdf/SKILL.md).
 */
val MgAccent = Color(0xFF32CC00)
val MgAccentHover = Color(0xFF28A300)
val MgAccentSoft = Color(0xFFE8F8E0)
val MgBg = Color(0xFFFFFFFF)
val MgBgSoft = Color(0xFFF7FBF4)
val MgBorder = Color(0xFFE4EDE0)
val MgText = Color(0xFF1A2B14)
val MgTextDim = Color(0xFF5A6B54)
val MgTextMute = Color(0xFF8A988A)
